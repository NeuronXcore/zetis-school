"""Module capsules : génération d'un `CapsuleSpec` typé par IA (ADR-0007).

Slice A = schéma strict + prompt versionné + service de génération (backend pur).
Pas d'endpoint ni de rendu Remotion ici (slice B / Lot 1)."""
