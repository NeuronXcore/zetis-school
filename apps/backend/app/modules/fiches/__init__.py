"""Module fiches (ADR-0015) : fiches de révision dérivées du cours canonique validé.

Slice A (backend) : type partagé `FicheSpec`, prompt versionné, génération (leçon forcée +
complément RAG, patron quiz), table `fiches` + endpoints Papa/Massimo. La UI est la Slice B.
"""
