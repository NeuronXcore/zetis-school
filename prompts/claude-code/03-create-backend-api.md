# Prompt Claude Code — Backend API FastAPI

Lis `CLAUDE.md`, `ARCHITECTURE.md`, `API_SPEC.md`, `DATA_MODEL.md` et `docs/backend/README.md`.

Crée le backend FastAPI avec :

- structure modules ;
- configuration ;
- connexion DB ;
- healthcheck ;
- auth placeholder ;
- routes subjects ;
- routes missions ;
- routes progress ;
- routes quizzes ;
- schémas Pydantic ;
- tests healthcheck.

Ne pas implémenter encore les appels IA réels. Créer seulement l’interface de jobs.
